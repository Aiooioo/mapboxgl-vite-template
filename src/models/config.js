import { defineStore } from 'pinia'

const useConfig = defineStore('config', {
    state() {
        return {

        };
    },

    actions: {
        initGlobalConfig() {

        }
    }
})

export { useConfig }
